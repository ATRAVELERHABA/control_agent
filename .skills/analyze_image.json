{
  "id": "analyze-image",
  "name": "图像识别",
  "description": "允许模型分析用户上传、拖拽或粘贴到对话中的图片附件，输出图像内容、界面布局与 OCR 文本。",
  "type": "tool",
  "enabled": true,
  "tool": {
    "name": "analyze_image",
    "description": "分析已注册的图片附件。适用于识别截图、照片、图表、界面结构和图片中的文字。",
    "requires_confirmation": false,
    "parameters": {
      "type": "object",
      "properties": {
        "assetId": {
          "type": "string",
          "description": "需要分析的图片附件 asset_id。"
        },
        "task": {
          "type": "string",
          "description": "可选的额外分析要求，例如只提取文字、只解释图表等。"
        },
        "ocr": {
          "type": "boolean",
          "description": "是否强化 OCR 输出，默认 false。"
        }
      },
      "required": ["assetId"],
      "additionalProperties": false
    }
  }
}
